# Optional: an openFDA API key raises the rate limit (240 → 120k req/day)
# and doubles the per-request batch size. Get one free at:
#   https://open.fda.gov/apis/authentication/
# Users can also paste a key directly in the UI's advanced filters.
FDA_API_KEY=
